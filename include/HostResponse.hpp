// AUTOGENERATED FILE - DO NOT MODIFY!
// This file generated by Djinni from com_verifone_commerce_host_adapter.djinni

#pragma once

#include <memory>
#include <optional>
#include <psdk/export.h>
#include <string>
#include <unordered_map>

namespace verifone_sdk {

enum class HostDecisionType;
enum class HostResponseType;
struct Decimal;

class PSDK_EXPORT HostResponse {
public:
    virtual ~HostResponse() {}

    /**
     * @param authorizationCode The Authorization code sent from the host.
     * @param hostDecision Enum representing the Hosts decision for the request, see {@link HostDecisionType}.
     * @param emvTags EMV Tags to be sent to the POI deviceAuthAmount.
     * @param authAmount The amount which has been authorized, including, if applicable, the approved cashback amount.
     * @param type The type of the host response, see {@link HostResponseType}.
     * @param signatureOverride Signature capture behavior: true means always capture, false means never capture, unset means follow configured behavior.
     */
    static std::shared_ptr<HostResponse> create(const std::string & authorizationCode, HostDecisionType hostDecision, const std::unordered_map<std::string, std::string> & emvTags, const Decimal & authAmount, HostResponseType type, std::optional<bool> signatureOverride);

    /** Authorization code sent from the host. */
    virtual std::string getAuthorizationCode() const = 0;

    /** Enum representing the Hosts decision for the request, see {@link HostDecisionType}. */
    virtual HostDecisionType getHostDecision() const = 0;

    /** EMV Tags to be sent to the POI deviceAuthAmount. */
    virtual std::unordered_map<std::string, std::string> getEmvTags() const = 0;

    /** The amount which has been authorized, including, if applicable, the approved cashback amount. */
    virtual Decimal getAuthAmount() const = 0;

    /** Type of the host response, see {@link HostResponseType}, */
    virtual HostResponseType getType() const = 0;

    /** Signature capture behavior, */
    virtual std::optional<bool> getSignatureOverride() const = 0;
};

}  // namespace verifone_sdk
